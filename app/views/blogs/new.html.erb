<h1>create blog</h1>

<div class="row">
  <div class="col-sm-5">
    <%= form_for @blog, url: blogs_path, method: :post do |f| %>
      <div class="form-group">
        <ul class="list-unstyled">
          <% @blog.errors.messages.values.flatten.each do |error| %>
            <li><i class="fa fa-exclamation-circle"></i> <%= error %></li>
          <% end -%>
        </ul>
      </div>

      <div class="form-group">
        <%= f.text_field :title, class: "form-control", placeholder: "title" %>
      </div>

      <div class="form-group">
        <%= f.text_area :content, class: "form-control", placeholder: "content" %>
      </div>

      <%= f.submit "save", class: "btn btn-st btn-primary btn-block" %> 
     
    <% end -%>
  </div>

  <div class="col-sm-7">
  </div>
</div>